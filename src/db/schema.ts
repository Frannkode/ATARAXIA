// El modelo de datos (product, category, productImage, etc.) se define en la
// Historia 1.2 — Modelo de datos base. Este archivo queda como stub para que
// drizzle-kit y src/db/index.ts tengan un módulo válido desde el arranque.
export {};
